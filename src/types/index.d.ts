export type Params = {
  verbose: boolean;
  configPath?: string;
  keep?: boolean;
  namedExports: boolean;
};

export const COMPILED_CSS_PREFIX = '__postcss__';
export const TYPE_DEF_FILE_EXT = 'd.ts';
